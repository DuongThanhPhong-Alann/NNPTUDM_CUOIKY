{
  "name": "nnptudm-cuoiky-mongo-schemas",
  "version": "1.0.0",
  "private": true,
  "description": "MongoDB (Mongoose) schemas for APT-CONNECT rewrite",
  "main": "src/models/index.js",
  "scripts": {
    "check:schemas": "node --check src/models/index.js",
    "db:check": "node scripts/check-db.js",
    "db:init": "node scripts/init-db.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "mongoose": "^8.16.0"
  }
}
